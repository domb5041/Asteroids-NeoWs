import React from 'react';

export default ({ data, height, selectX, selectY, width }) => (
    <svg className='container' height={height} width={width}>
        {/* Our visualization should go here. */}
    </svg>
);
